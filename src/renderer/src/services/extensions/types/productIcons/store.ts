/**
 * Tipo 'productIcons' — store: re-exporta la persistencia del servicio global.
 */

export {
  loadStoredActiveProductTheme,
  saveStoredActiveProductTheme,
  clearStoredActiveProductTheme
} from '@services/productIcons/store'
