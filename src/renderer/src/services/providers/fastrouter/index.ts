import type { ProviderConfig } from '../types'

export const config: ProviderConfig = {
  id: 'fastrouter',
  name: 'FastRouter',
  description: 'FastRouter — LLM provider.',
  baseUrl: 'https://go.fastrouter.ai/api/v1',
  apiKeyUrl: '',
  defaultModel: 'wanx/wan-v2-6'
}

export default config
