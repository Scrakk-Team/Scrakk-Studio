import type { ProviderConfig } from '../types'

export const config: ProviderConfig = {
  id: 'llmtr',
  name: 'LLMTR',
  description: 'LLMTR — LLM provider.',
  baseUrl: 'https://llmtr.com/v1',
  apiKeyUrl: '',
  defaultModel: 'sincap'
}

export default config
