import type { ProviderConfig } from '../types'

export const config: ProviderConfig = {
  id: 'v0',
  name: 'v0',
  description: 'v0 — LLM provider.',
  baseUrl: 'https://api.v0.com/v1',
  apiKeyUrl: '',
  defaultModel: 'v0-1.0-md'
}

export default config
