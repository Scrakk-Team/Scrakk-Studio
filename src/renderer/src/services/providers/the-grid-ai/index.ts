import type { ProviderConfig } from '../types'

export const config: ProviderConfig = {
  id: 'the-grid-ai',
  name: 'The Grid AI',
  description: 'The Grid AI — LLM provider.',
  baseUrl: 'https://api.thegrid.ai/v1',
  apiKeyUrl: '',
  defaultModel: 'agent-prime'
}

export default config
